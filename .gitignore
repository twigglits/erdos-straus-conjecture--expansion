# OS / editor cruft
.DS_Store

# Python bytecode (from running the analysis scripts)
__pycache__/
*.pyc

# Rust build artifacts (engines/fpr.rs is compiled directly with rustc)
target/
**/*.rs.bk

# local binaries (regenerable; see REPORT.md §8 for build commands)
engines/fp
engines/fpr
engines/fpcuda
engines/fsigned
analysis/type3_kappa_c

# transient run output and validation scratch
*.partial
*.err
engines/err_*.log
engines/cuda_*.csv
engines/got_*.csv
engines/ref_*.csv

# Lean build artifacts (Mathlib cache, ~GBs — regenerate with lake exe cache get)
erdos1/subsetsums/.lake/

# --- Local scratch (regenerable; not deliverables) ---
# Engine binaries + transient run output if dumped into a files/ scratch dir
# (engines/* + *.partial + *.err above catch most; build commands in REPORT §8).
files/
# Firecrawl scrape cache (erdosproblems.com pulls) — regenerated on demand.
.firecrawl/

# §16 targeted-floor intermediate artifact (regenerable via analysis/target_1e10.py)
data/target_1e10_bottomK.txt
engines/fp128
engines/fp_single
